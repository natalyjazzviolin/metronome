import { HrzToPitchPipe } from './hrz-to-pitch.pipe';

describe('HrzToPitchPipe', () => {
  it('create an instance', () => {
    const pipe = new HrzToPitchPipe();
    expect(pipe).toBeTruthy();
  });
});
